import { COLOR } from 'react-native-material-ui';

export const MENU_BAR_COLOR = COLOR.amber400;
export const BUTTON_COLOR = COLOR.blue400;
export const SECONDARY_TEXT_COLOR = COLOR.white;

const uiTheme = {
  palette: {
    primaryColor: MENU_BAR_COLOR,
    accentColor: BUTTON_COLOR,
    secondaryTextColor: SECONDARY_TEXT_COLOR,
  },
};

export default uiTheme;
