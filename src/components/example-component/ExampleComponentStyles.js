import { StyleSheet } from 'react-native'

const ExampleContentStyles = StyleSheet.create({
  container: {
    padding: 100,
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  helloWorld: {
    margin: 30,
    backgroundColor: 'red',
  }
});

export default ExampleContentStyles;
